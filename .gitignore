# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual Environment
venv/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Data (too large for git — download separately)
data/raw/
data/processed/
data/tokenizer/*.model
data/tokenizer/*.vocab

# Model checkpoints (too large for git)
# checkpoints/  <- Allowing the folder so we can push training_history.json
*.pt
*.pth
*.bin

# Logs
# logs/         <- Allowing logs so they can be pushed
# runs/

# Plots (regenerated from training history)
# plots/        <- Allowing plots so they can be pushed

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/
